# Portrait du personnage (BasicInfo) et moteur de capture

> Journal du chantier. La fiche de mémoire `project_char_portrait_re` n'en garde que l'état et les règles.
> Extrait le 2026-09-04 de la mémoire ; contenu historique, non revérifié.

## 🔴 2026-08-03 — LE MOTEUR DE CAPTURE N'EXISTE PLUS

Tout ce qui suit décrit un mécanisme **SUPPRIMÉ** : faire rendre le personnage
par le renderer natif et intercepter ses calques par un hook sur le quad-submit
(`FUN_00a1b7c0`). Hook, `CapLayer`, `CapturePortraitActor`, `CaptureAvatarActor`
et leurs tampons ont été retirés (~800 lignes). **Ne pas s'y référer pour du
code neuf** — c'est conservé pour les leçons de méthode, pas comme recette.

Le portrait, l'avatar plein-corps et l'export GIF passent tous par
`ro::DrawDoll` ([[project_doll_composer]]). Ce que le composeur a fallu ajouter :

* `measure_only` + les boîtes tête/corps dans `DollPlacement` — ImGui dessine
  dans l'ordre des appels, donc on ne peut pas recadrer après coup : on mesure,
  on décide, on dessine ;
* `place_override` — imposer origine et échelle ;
* `force_frame` — viser une image nommément (le GIF ; l'horloge ne permet pas de
  viser, l'échantillonner sauterait ou doublerait des images) ;
* `quad_sink` — recevoir les calques au lieu de les peindre.

⚠ `D3D9TexQuad` prend désormais **quatre coins**. Les calques d'arme et de
bouclier sont TOURNÉS par leur `.act` ; un `x0,y0,x1,y1` les redressait.

« Tête seule » est un choix de **CADRAGE**, plus de composition : le pantin est
composé entier (la tête s'ancre au corps, les coiffes à la tête), on zoome sur la
boîte de la tête et le reste sort du clip.

---

**STATUS 2026-06-28: WORKING — the head portrait renders (head/face+hair), live-confirmed by the user.** Core goal met: regenerated head sprite + pseudo/class/level + movable per-element frames + hide native Basic Info. Head-only mode (skip the body) ON by default. Zoom slider 0.10–2.00 (def 1.0) + vertical-offset slider for framing. Per-layer capture diagnostics are opt-in (`portrait_debug_log_` checkbox "Debug log", off by default — LogDiag every 2s to bourgeon.log). Build B5E5C4EE. **HEAD-GEAR offset fix v2 = per-layer ACT anchor read in the quad hook (build C01739FC).** The v1 (hook FUN_0070f4b0 to stash g_cur_anchor) had a 1-section LAG (head got body's sync, head-gears got head's) because FUN_007adbc0 makes extra FUN_007ae170/FUN_0070f4b0 calls during the head. v2 reads each layer's anchor RIGHT IN Hooked_ActorQuad with NO lag: **param_3 (p3) of FUN_00a1b7c0 IS that layer's ACT object** (= local_40, the same the game passes to FUN_0070f4b0). So `ReadActAnchor(p3)` calls FUN_0070f4b0(p3, actor.dir@+0x38, actor.action@+0x3c) directly (NOT hooked now — removed the FUN_0070f4b0 hook), reads anchor @frame+0x34 ({x,y}, count@+0x40). First layer = body = g_body_anchor. `sync = body_anchor - layer_anchor`; `cx=(act_layer[0]+sync_x)*scale`, cy likewise. Needs g_cur_actor (set in CapturePortraitActor) for dir/action. Diag logs sync per layer (head should be ~(2,-1)). User chose to keep the synthetic actor (vs capturing the real render). UNVERIFIED live — needs test. (NOTE: user pet peeve — I twice declared victory prematurely; verify by screenshot/log before claiming success.)

**HEAD-GEAR offset fix via ACT-anchor sync (build 4A830C73).** After the int fix, the HEAD aligned (game raw=(2,-1)) but HEAD-GEARS still arrived raw=(0,0) — the game's anchor sync is reliable only for body+head, not head-gears. So we now COMPUTE the moonlightsite makeheadhat sync ourselves: per layer, `centre = (layer.posi - (section.ACTanchor - body.ACTanchor)) * scale`, IGNORING the game's raw for positioning. Implementation: hook **FUN_0070f4b0** (0x0070f4b0, ACT frame getter) — while g_cap_active, read the returned frame's anchor (ptr @frame+0x34 = {x@0,y@4,type@8}, count @frame+0x40>0; confirmed via FUN_007adbc0) into g_cur_anchor; the FIRST section in a pass = body = g_body_anchor (reference). In Hooked_ActorQuad: cx/cy = (act_layer[0/1] - g_cur_anchor + g_body_anchor)*scale. Diag logs `sync=(sx,sy)` per layer for verification. (NOTE: the earlier costume "cat" confusion was a Costume Norwegian Forest Cat (viewID 2701, Upper/Mid/Low) the user had on — not Doram; costume precedence read it. Removed costumes = normal char. UNVERIFIED live yet — needs the user to test + send the sync log.)

**THE REAL OFFSET ROOT CAUSE (build 0EBE2751, found via 2nd workflow wf_a4240957 — failed at synth but findings recovered from journal.jsonl; live-debugger evidence, high conf): an ABI TYPE BUG in our hook.** FUN_00a1b7c0 param_1(x)/param_2(y) are **SIGNED INT pixel offsets** (they ALREADY carry the game's body-anchor sync, often negative), NOT floats. We declared `float y` → reading int -1 (0xFFFFFFFF) as a float = NaN. The body pushed 0x00000000 = 0.0 (valid float) so only the body looked clean; rawx was fine only because we read param_1 via void*/intptr (int). So the head was ~1px off and head-gears badly off, and our "NaN→0 clamp" threw away the real sync. **FIX: hook signature x/y = `int` (not void*/float), `rawx=(float)x; rawy=(float)y;` (real int→float cast), DELETE the NaN clamp.** No ctor change, no manual anchor recomputation — the game's `-(anchor-body_anchor)` term is already in the int rawy we were corrupting. (Live proof: head submit slot [ESP+8]=0xFFFFFFFF=-1; body=0x00000000; call site CVTTSS2SI→int→PUSH, no CVTSI2SS. FUN_007adbc0 stored this+0x8c/0x90 = head.anchor-body.anchor = (2,-1) for dir0.) Fallback if ever needed: agent ad867ac6 mapped the ACT anchor (frame=FUN_0070f4b0(act,dir,action); anchor ptr@frame+0x34={x,y,type}, count@frame+0x40; layer=FUN_0070f390; ACT obj per layer-type via FUN_007ae170(actor,&out,type)->out[2]) to compute the sync manually — not needed with the int fix.

**Head-only rework + framing (build A6468CE9):** ALL layers now have raw≈0 (anchor accumulation clamped) so composition is purely by layer offset; head-only is NOT a capture skip anymore — capture ALL layers, and at DRAW time skip the **body = the first captured layer g_caps[0]** (back-most; drawn first). Keeps head+hair+head-gears, drops the body (so "head mis-aligned to body" is moot in head-only). Framing sliders: Zoom (0.10-2.0), **Décalage horiz. portrait_head_offx_ (def 0.5)** + Décalage vert. portrait_head_offy_ (def 0.10) — focus = top-centre of the kept bbox. **DORAM = par'd aside (user: doesn't work right; special sprite layout).** **Body stays Novice for normal jobs in full-body mode** (FUN_00d5bc30->FUN_00b43af0->FUN_00d99150(job,weapon,1) resolves the body class; novice fallback — the HEAD is hair-based so always right; user hint: maybe ROBE/ROBE2 alt-skin naming) — NOT fixed but MOOT for the head portrait (body hidden in head-only). 

Head-gears ADDED (build D1EBAE51): read the equipped headgear view ids straight from the session equipped-item array — **item[slot] = session(0x015fa3c0) + 0x17d0 + slot*0xf8; view id @ +0x70, unique-slot tag @ +4** (live-verified: slot8/9/0 = head-mid/low/top; e.g. High Priest views 1079/661/8). De-dup by the +4 tag (per FUN_008cf970), pass hg_top/mid/low to FUN_007ac210 (params 9/10/11). They render as cases 3-6, anchor to the head (NaN raw → kept in head-only). Remaining polish: framing fine-tune. **KEY LESSON (user-driven): instrument with LogDiag (warn-level) to get ground truth BEFORE theorizing — it resolved in one pass what 3 wrong hypotheses (+0x24 head-gate, act_layer[8] RGBA skip) could not (the real cause was a NaN head Y position).**

GOAL (2026-06-28): regenerate the player's HEAD sprite on screen as a movable "status portrait" (with pseudo + base/job level nearby), in the continuation of the BasicInfo tweak project; finally offer to HIDE the native Basic Info window (UIBasicInfoWnd id 0). New plugin planned: `PortraitTweaks` (src/plugins/portrait.{cc,h}). 20250716 client, base 0x400000, Ghidra==live.

## The character/actor renderer (= the equip-window "doll", reusable)
The equip window draws the player as a composited actor (body+head+hair+headgears+weapon...). DrawContent `FUN_008b3190` does, once equipped-item fields are filled:
```
obj = FUN_007ac210(<52+ byte buf>, renderCtx=equipWnd, x=width/2+1, y=0x95,
        sex, jobShort, jobFull, weaponShort, hair, hgTop, hgMid, hgLow, garment,
        0, DAT_0160240c, clothesColor, hairColor, dir=0, action=0, *(equipWnd+0xd4));
FUN_007ac820(obj, '\0');   // draws all layers
FUN_0079a6a0(buf);          // destructor
```
- **Actor_Init @0x007ac210** (ex-FUN_007ac210, vtable **PTR_FUN_0101dac8**). Field map CONFIRMED from the real caller **Actor_DrawFromCharInfo @0x0079ab80** (char struct=param_1): +0x04 owner, +0x08 p2, +0x0c p3, +0x10 sex(byte, char+0xae), +0x12 **job**(ushort, char+0x54), +0x14 **job** again (signed short, SAME char+0x54), +0x18 **job_body**(ushort, char+0x58 — a 2nd job copy, NOT a weapon!), +0x1a hair(char+0x56), +0x1c/0x1e/0x20 hg top/mid/low(char+0x60/0x64/0x66), +0x24 garment(char+0xa2), +0x28 baby flag, +0x2c altView gate(0=draw face/garment), +0x30 clothesColor(char+0x6a), +0x34 hairColor(char+0x68), **+0x38 direction(param_2[0x53]), +0x3c action/pose(param_2[0x54])**, +0x40 p19. Builds layer-priority list ("IsHelmRobe","TB_Layer_Priority"). Object ~0x98 bytes.
- **⚠️ BODY=NOVICE BUG RÉSOLU (2026-06-28):** the body class is **NOT** taken from +0x12. case 1 of Actor_BuildSpriteLayers(0x007ae4e0) → **Job_GetBodySpritePath(0x00d5bc30)** → **Job_BuildBodySpritePath(0x00b43af0)** → `iVar5 = Job_ResolveBodyClass(job=this+0x12, job_body=this+0x18, 1)`. **Job_ResolveBodyClass(0x00d99150) RETURNS param_2 (=this+0x18) remapped**; param_1 only picks the tier. For a 3rd job (HP 0xFA9=4009): tier 0, no remap, then `param_2>0xf6e → param_2-0xf6e` = 4009-3950 = **59 = HP body class**. If this+0x18=0 → returns 0 → **Novice**. Fix in basic_info.cc CapturePortraitActor: pass `job & 0xffff` into the +0x18 slot (was `weapon=0`). Helpers: **Job_ToBabyClass 0x00d83d60** (job→baby), **Job_ToBaseClass 0x00d9a110** (job→base/normalise), **Job_GetGarmentSpritePath 0x00d846f0** (case 0/7 garment layer, id 0 ⇒ not drawn).
- **Actor_DrawSprites @0x007ac820** (ex-FUN_007ac820, `__thiscall(this, char param_1)`) = draw. Loops layers; per layer gets ACT frame (Act_GetFrame/Act_GetFrameLayer), sprFrame `*(int*)(*(int*)(sprObj+0x510 + layer[8]*0xc) + layer[2]*4)`, atlas tex via **SpriteAtlas_GetCachedTexture 0x00566b70** (already hooked for the RO cursor → CTexture+0x12c = IDirect3DTexture9*, +UV geom) / SpriteAtlas_BuildTexture 0x005663d0, then submits the quad. **param_1==1 path → Actor_SubmitSpriteQuad 0x00a1b7c0**; **param_1==0 path** (equip) → `renderCtx+0x24 -> vtable[0x28]()`. Both funnel to the batched RenderQueue (drawn in the game pass, BEHIND ImGui — same as the cursor).
- **⚠️ HEAD-ON-BODY ANCHOR + ANIMATION RÉSOLU (2026-06-28, workflow adversarially-verified):**
  - **Attach is in the quad x/y, NOT in act_layer.** For the hair/head layer (case 2) Actor_DrawSprites calls **Actor_ComputeHeadAttach 0x007adbc0** = `headFrame.Pos[0] − bodyFrame.Pos[0]` (only when the anchor KEYS match: `bodyAnchor[2]==headAnchor[2]`); the result is summed into the position accumulator local_30/local_24 and arrives folded into Actor_SubmitSpriteQuad's x/y (`x=local_30*scale+this+8`, `y=this+0xc+local_24*scale`). Headgears (cases 3-6) chain the same diff against the HAIR anchor (body→head→headgear). So the capture hook must just **use x/y** + replay the rect math — the old per-layer "sync = body_anchor − layer_anchor on act_layer" approach was wrong (double-counting / wrong term) and is removed.
  - **Quad rect formula (Actor_SubmitSpriteQuad 0x00a1b7c0, no-rotation param_8==0, CONFIRMED pixel-exact):** `left=x+L[0]*scale*f5; right=x+((spr[2]+1)*spr[0]+L[0]−1)*scale*f5; top=y+L[1]*scale*f6; bottom=y+((spr[3]+1)*spr[1]−1+L[1])*scale*f6` then **+0.5** on every coord. `L`=act_layer (the 0x24-byte FRAME-LAYER from Act_GetFrameLayer): L[0]/[1]=x/y int, L[2]=sprIdx(-1=skip), L[3]&1=mirror, L[5]=scaleX **float-reinterpret**, L[6]=scaleY **float-reinterpret**, L[7]=angle, L[8]=sprType(0=palette). `spr`=spr_frame short*: [0]=tileW,[1]=tileH,[2]=tilesX-1,[3]=tilesY-1. p11 caller-arg=1.0. Entry folds renderer origin `x+=this+0x1c; y+=this+0x20` (int, common to all layers → cancels for relative layout).
  - **Animation (idle combat = STANDBY):** `this+0x38 = animType*8 + dir` (low 3 bits = dir, `&0x80000007`), `this+0x3c = frame index`. **Standby animType = 4 → pose 32** (dir 0 front; confirmed by 3 independent code paths incl. Actor_AnimateAndSelectStandby 0x00603f00 `MOV ESI,0x4`). Frame struct = 0x44 bytes; **numFrames = (Act_GetActionFrames(act, pose)[1] − [0]) / 0x44**. ACT layout: actions vector act+0x110/+0x114 (0xc-byte descr, idx=animType*8+dir), each → frames array (0x44 each); frame+0x20/+0x24 = layer vector (0x24 each), frame+0x34 = Pos[] anchor ptr, frame+0x40 = Pos count, per-frame delay vec act+0x12c/+0x130 (default interval act+0x11c). Ghidra renames: Act_GetActionFrames 0x0070f2c0, Act_GetFrame 0x0070f4b0, Act_GetFrameLayer 0x0070f390, Act_GetFrameCount 0x0070f6b0, Act_GetFrameDelay 0x0070f3d0, Actor_SelectLayerAnimType 0x00d38850.
  - Impl: hook in basic_info.cc rebuilt to use x/y + the rect formula; CapturePortraitActor passes pose=32 and a time-cycled frame (~1.3 s loop), capturing the body frame count from the first layer's ACT in the hook.
- **WEAPON / SHIELD / GARMENT (workflow 2026-06-28, archV verified):**
  - **Garment = EASY, done.** The body actor DOES draw it: Actor_BuildSpriteLayers case 0/7, src = actor field **this+0x24** (plain view id, char+0xa2), gated by this+0x2c==0 (we pass 0). Source global = **g_OwnLook_GarmentRobeViewId 0x015fb2a0** (like hair 0x015fb278; written by Net_OnSpriteChange_UpdateLookGlobals 0x00cd8e70 case 0xc LOOK_ROBE, read by World_SpawnOwnPlayerActorFromLookGlobals 0x00cf2e70). basic_info.cc feeds it into the ctor garment slot (only in full-body mode). Garment palette resolver Job_GetGarmentPalettePath 0x00d84680 / impl Job_BuildGarmentSpritePath_impl 0x00b442f0.
  - **Weapon & shield are NOT on the body actor** — confirmed across all 10 Actor_DrawSprites callers (even the equip doll shows none). They live on a SEPARATE high-level **CActorSprite** object (vtable family 0x01093b48): weaponView @+0x440, shieldView @+0x444, sprite slots this+0x4b8[5]=main /[6]=trail, hand-anchor offset this+0x4dc/+0x4e0. Built by CActorSprite_BuildWeaponLayers 0x00d403a0 / CActorSprite_BuildShieldWeaponLayers 0x00d412a0 → CActorSprite_SetSlotSprite 0x00d3d7c0 (computes anchor = diff weapon-frame anchor[1] vs body-frame anchor[1]); rebuilt via CActorSprite_SetSexAndRebuildEquip 0x00d3dc00 (vtbl+0x60). Drawn by CActorSprite's own render path — **NOT** Actor_SubmitSpriteQuad, so our hook never sees it.
  - View ids: **g_OwnLook_WeaponViewId 0x015fb280** (live 0x63f=1599), secondary 0x015fb288, **g_OwnLook_ShieldViewId 0x015fb284** (live 0x897=2199). Weapon path resolver **Job_BuildWeaponSpritePath 0x00b456c0** (wrappers Job_GetWeaponSpritePath 0x00d8a010 SPR / Job_GetWeaponActPath 0x00d8a160 ACT); weaponClass = Weapon_ItemIdToWeaponClass 0x00d8a1d0 → Weapon_ResolveTypeBucket 0x00d39130.
  - Equip-array slot scheme (session 0x015fa3c0 + 0x17d0 + slot*0xf8, view@+0x70; costume +0x2b30): head-top=0, **weapon=1**, **garment=2**, **shield=5**, head-mid=8, head-low=9 (weapon/shield +0x70 is 0 → use the LOOK globals for the numeric id). Readers FUN_00d59b90 (general) / FUN_00d800b0 (costume) via FUN_0089e380.
  - ⛔ ~~TODO option A (weapon/shield in the portrait)~~ — **ABANDONNÉ (2026-07-30)** :
    ne vaut pas le coup (visible seulement en pose combat + mode plein-corps, or le
    portrait est tête-seule par défaut). La machinerie EXISTE et tourne dans
    l'AVATAR de la fiche perso : `EmitWeaponShieldLayers` (basic_info.cc, slot-live
    sur l'acteur joueur +0x4ac/+0x4b8, slots 5/6/7) + z-order directionnel — si un
    jour on le veut au portrait, c'est ~20 lignes dans CapturePortraitActor.
- **Portrait settings now:** animation selector (animType combo, default Combat/standby 4), Direction (0-7), Animer (play/freeze), Cape/garment toggle (full-body only), CTRL+drag = block-move touching frames (PortraitTouchGroup transitive overlap), all settings sliders wheel-adjustable (WheelSliderFloat/Int via SetItemKeyOwner). Debug-log toggle removed from UI.
- char-preview CONTROL ctor `FUN_008360c0` (vtable PTR_FUN_0102c728) = the equip this+0x120 viewport; NOT the actor renderer itself.

## Player appearance source (FUN_008cf970 @0x008cf970, own-player path obj+0xb4==0)
Fills the equip window fields that feed FUN_007ac210. Replicate these reads to build a portrait actor:
- jobid   = `FUN_00d5b580(0x15fa3c0)` (the universal GetJobId(session); session=0x015fa3c0)
- sex     = `FUN_00d84760(0x15fa3c0)`
- hair    = `DAT_015fb278`
- clothes color = `DAT_015fb28c` ; hair color = `DAT_015fb290`
- weapon  = `FUN_00a75340(0x1213338)`->+0xcc->+0x2c->+0x4c8 (player object) — guarded
- headgears: `FUN_0089e380(equipWnd,&buf, slot)` slot 8→hgMid(0x108), slot 9→hgLow(0x10c), slot 0→hgTop(0x104); garment via FUN_0089e380(...,2)+FUN_006a25f0 (0x118). FUN_0089e380 needs a UIWnd `this`; for a standalone portrait, find the slot/equip read that doesn't need the equip window (or just open-source from the session view-equip globals DAT_015ffd18.. used in the other-player branch).

## Name + level globals (CONFIRMED via UIBasicInfoWnd_DrawContent disasm 0x0095e620)
- **Base level = `DAT_015fb9f0`** (pushed before "Base Lv. %d" @0x0095ef03 / collapsed bar @0x0095e971).
- **Job level  = `DAT_015fb9f8`** (pushed before "Job Lv. %d"  @0x0095ef53).
- CLASS name (job name string) = `FUN_00d5bb40(&DAT_015fa3c0, jobid, -1)` (indexes a class-name table at session+0xf88; NOT the character nickname).
- ~~Pseudo (character nickname) = STILL TODO~~ — **RÉSOLU plus bas** (§ « Pseudo =
  SOLVED ») : `session().GetCharName()` @session+0x81A8, utilisé par l'élément
  kPortName du portrait. FUN_00d5bb40 reste le nom de CLASSE, pas le pseudo.
  (Base/job EXP cur/max: DAT_015fb9d0/d8 base, d_e8/e0 job; HP/SP 015ff908/90c/910/914 — see [[project_basic_info_menu_icons]].)

## Rendering approach (DECISION PENDING live validation)
Three options weighed; existing plugins favor ImGui windows + sprite capture (see [[project_ro_cursor]] capture-hook 2b, [[reference_imgui_game_textures]]):
1. **Capture-hook (preferred for full ImGui integration / movable window / correct z-order):** build own actor object (front idle), render once/frame with a capture flag; hook FUN_00a1b7c0 (param_1==1 path) or reuse the SpriteAtlas_GetCachedTexture hook to grab per-layer (CTexture+0x12c, UV, dest, color, mirror); suppress the native submit; re-draw head layers via ImGui window draw list, cropped to the head region (skip body/weapon/shield). NEEDS live x32dbg to validate a safe renderCtx for FUN_007ac820 (equip passes its UIWindow; vtable+0xa0 getter is called before submit) + the capture point.
2. **Transparent ImGui window over native sprite:** render actor natively at the window's screen pos; ImGui window NoBackground so the (earlier-drawn, behind) sprite shows through; draw only border+name+level in ImGui. Simpler but: needs a valid renderCtx, sprite lags 1 frame on drag, position must track exactly.
3. Native-only new UIWindow: too hard (new window class/vtable/factory).

## Pseudo (character name) = SOLVED via existing API
`Bourgeon::Instance().client().session().GetCharName()` (Session, src/ragnarok/session.cc) reads char_name_ at **session+0x81A8** (0x015fa3c0+0x81A8 = 0x01602568; live = "Stingor"). No new RE needed. Live-confirmed in-game: base lvl 0x015fb9f0=999, job lvl 0x015fb9f8=80, hair 0x015fb278=7, clothes col 0x015fb28c=56, hair col 0x015fb290=76.

## Plan (staged)
- **Stage 1+ = DONE & BUILT+DEPLOYED (2026-06-28).** Merged INTO **BasicInfoTweaks** (src/plugins/basic_info.{cc,h}), NOT a separate plugin (user: "tu met pas ça avec basic_info plutot?"). REFACTORED per user to **INDEPENDENT movable elements** ("pouvoir deplacer tous les element indépendament … chacun sa config de placement, couleur/opacité de background, frame rounding"): `struct PortraitElem {show,x,y,w,h,bg[4],fg[4],rounding}` + `enum PortId {kPortHead,kPortName,kPortClass,kPortLevel}` + `ports_[kPortCount]`. `DrawPortraitElem(id)` = one standalone NoBackground frame each, custom move + edge/corner resize + grid-snap (reuses the bars' drag_mode_/drag_edges_/drag_off_* members — only one widget active at a time), own bg colour/opacity + rounding + text colour. Head = placeholder box; name=GetCharName(); class=ClassName(); **level merged = "%d/%d"** (base/job). `DrawPortrait()` loops shown elems, persists on drag-end via geometry_dirty_. Persisted by MoonlightUi under per-element keys `portrait_<head|name|class|level>_<show|x|y|w|h|rounding|bg|fg>` + `portrait_visible/locked/hide_basic_info`. Settings = "Portrait" tab (per-element show/bg/fg/rounding + master toggles).
- **Stage 3 = hide native Basic Info = DONE.** `BasicInfoTweaks::OnTick()` pins UIBasicInfoWnd off-screen while `portrait_hide_basic_info_` on. **LIVE-CONFIRMED:** BasicInfo singleton = `*(0x0131f6c4)` (= g_UIWindowMgr 0x0131f4e8 + 0x1dc), vtable 0x0103e35c; live pos +0x1c/+0x20 (was (0,0)); a direct write of +0x1c/+0x20 to (-10000,-10000) STICKS (game doesn't reset it). OnTick captures the real pos once, pins off-screen each tick, restores on disable. Direct field write (not SetPos) so the docked menu-icon grid is NOT dragged off with it. (Untested visually whether the composite follows the field write — verify next session; fallback = SetPos vtable+0x10 which definitely moves it but drags the icons too.)
- **Stage 2 = head sprite = IMPLEMENTED & COMPILES (2026-06-28; UNTESTED live — needs the game closed to deploy, then tuning).** In src/plugins/basic_info.cc: `CapturePortraitActor()` builds the player actor via FUN_007ac210 (renderCtx = the BasicInfo window `*(0x0131f6c4)`; appearance = GetSex(0x00d84760) + jobid(0x00d5b580) + hair DAT_015fb278 + clothes DAT_015fb28c + hair-col DAT_015fb290; **headgears/weapon = 0 in v1**; dir/action 0/0), then `FUN_007ac820(actor, 1)` (**param=1 → routes every layer through FUN_00a1b7c0**), then dtor FUN_0079a6a0. Hook `Hooked_ActorQuad` on FUN_00a1b7c0 (HookManager kJmpHook, lazy InstallActorCapture) captures each layer into g_caps[] when g_cap_active: tex = SpriteAtlas_GetCachedTexture(atlas=*(0x012515f8)+0xc0, sprFrame, palette, geom)->CTexture+0x12c (DX9), UV=geom[3..6], size=sprFrame[0/1]*scale, centre=(x + actLayer[0]*scale, y + actLayer[1]*scale), mirror=actLayer[3]&1; then SKIPS the native submit (suppress). **GOTCHA: FUN_00a1b7c0 param_10 (palette) is a POINTER mis-typed `float` by Ghidra — receive it as void*, never a float cast.** Called from **OnTick** (update phase = safer than Present), SEH-guarded; DrawPortraitElem(kPortHead) composites g_caps fitted-to-width + top-anchored + clipped (head fills the frame). Toggle `portrait_head_sprite_` (default on); placeholder box if capture empty. **TUNING EXPECTED live:** sprite centre/offset convention (I used centre=anchor+layerOff*scale), fit/crop, and whether calling FUN_007ac820 from OnTick is stable. v2: add head-gears (FUN_00d59b90(session,&buf,slot) slots 8/9/0, viewid at buf+0x70) + maybe weapon.
- **TRUE ROOT CAUSE (found via LogDiag instrumentation, 2026-06-28 — the user told me to stop guessing & add logs; that was decisive).** Added per-layer LogDiag in Hooked_ActorQuad (count + type + WxH + tex-ok + off + raw + centre), throttled 2s, logged from a separate fn (CapturePortraitActor uses __try → no C++ unwinding allowed there). Log showed exactly **2 layers** reach the hook for our actor, BOTH type 0, tex=ok: **body 38x74 raw=(0,0)** and **head/face 31x28 raw=(2, -2147483648)**. So head IS captured — but its **raw Y = -2147483648 = INT_MIN = (int)NaN**: the head's neck-anchor math in FUN_007ac820 (fVar16 = local_24 * fScale, then (int)) goes Inf/NaN for our SYNTHETIC actor → head drawn at y≈-2.1e9 = OFF SCREEN. (x=2 and offset oy=-81 were valid; only the accumulated raw Y was garbage.) The head sprite/texture/size are all fine. **FIX (Hooked_ActorQuad): clamp a non-finite/absurd raw coord to 0** (`if (!(v > -1e6 && v < 1e6)) v = 0;`) so the layer offset alone positions it (body raw is clean (0,0)). Build F45C518C. NB: act_layer[8] is ALWAYS 0 here (body+head both palette/atlas) — the act_layer[8]!=0 branch I added is harmless/unused for these; keep it for future head-gears. So neither the +0x24 nor the act_layer[8] theories were the cause — it was a NaN position. **LESSON: instrument with LogDiag (warn-level, survives the info filter) before theorizing.**
- **(superseded) ACTUAL ROOT CAUSE … +0x24 FAILED (only added a stray head ACCESSORY/"wings", not the face).** RE'd the layer map authoritatively via **FUN_007ae4e0** (the per-node sprite resolver, switch on node type piVar13[2]): **case 1 = BODY** (FUN_00d5bc30, job+sex), **case 2 = HEAD/FACE (머리, UNCONDITIONAL, uses hair @+0x1a + FUN_00d5a480)**, **case 0/7 = head ACCESSORY** (gated +0x24>0, FUN_00d846f0), **cases 3-6 = head-gears** (from the +0x80 map). So the FACE (case 2) ALWAYS reaches our hook — it vanished because **its sprite is an RGBA-type frame (act_layer[8]!=0)**, and our capture only handled act_layer[8]==0 (the body atlas path) AND unconditionally suppressed the native draw → the face/hair were skipped+suppressed = invisible. (The earlier workflow "+0x24 gates the head" was about the case-0 ACCESSORY, not the unconditional case-2 face; live `+0x24=1` added wings, confirming.) **FIX (in Hooked_ActorQuad): handle act_layer[8]!=0 too** — the frame references its CTexture directly at `*(short*)spr_frame + 4` (= byte +8), native = CTexture+0x12c, UV = (0,0)..(spr_frame[0]/texW, spr_frame[1]/texH) where texW/H = CTexture+0xc/+0x10. param_12/garment reverted to 0. Build 37a8ffeb deployed. (Live field map re-confirmed: vtable 0x0101dac8, +0x12 jobShort, +0x1a hair, +0x24 accessory-gate, +0x30 clothesCol, +0x34 hairCol, +0x38 dir, +0x3c action.)
- **(superseded) earlier head-gate theory: FOUND & FIXED via param_12 — WRONG (only added an accessory).** The HEAD layer was DROPPED at ENUMERATION, never reaching our capture hook. In the actor ctor FUN_007ac210, **param_12 -> actor+0x24** (the slot I mislabeled "garment", passed as 0). The list builder **FUN_007ad0b0** (called first in FUN_007ac820) gates the human-head layer (case 0) on `(0 < *(this+0x24)) && (*(this+0x45) != 1)`, and the Doram head (case 7) on `0 < *(this+0x24) && *(this+0x45)!=0`. **LIVE CONFIRMED** (bp FUN_007ac820 cond ecx<0x400000 to catch our stack actor 0x1af178; vtable 0x0101dac8 ✓): our actor had **+0x24 == 0** (head never enumerated) and **+0x45 == 0** (so case-0 human-head path is active once +0x24>0). FIX = pass **param_12 = 1** (the head-gate; >0 is enough). Also confirmed live the field map is exactly per the ctor decompile: +0x12 jobShort, +0x1a hair, +0x24 garment/head-gate, +0x30 clothesCol, +0x34 hairCol, +0x38 dir, +0x3c action. **The earlier "act_layer[8]!=0 RGBA branch" theory was DISPROVEN** by the adversarial verify: FUN_007ac820 calls FUN_00a1b7c0 UNCONDITIONALLY per enumerated layer (act_layer[8] only selects atlas-vs-direct CTexture source, +0x12c either way); the head simply was never enumerated. (HAIR is case 2 = unconditional; if hair still missing after the +0x24 fix, check the per-node null spr/act early-out in FUN_007ac820 ~0x007ac896, or framing.) Build 23185672 deployed. Workflow run: wf_712c6081-626 (6 agents, high confidence).
- **LIVE TEST 1 (2026-06-28): sprite RENDERS (capture works!).** Breakpoint on FUN_007ac210 confirmed ALL ctor params correct: job=0xFCF=4047 Star Gladiator, sex=1, hair=7, clothes 56, hair-col 76, dir/action 0. Hook on 0x00a1b7c0 confirmed live (first byte E9). So it's NOT a novice — that's SG's plain TaeKwon-line outfit, and the user only saw the TORSO (no head/face). **Root cause = FRAMING: fitting the whole body to width made the head tiny.** FIX: zoom into the top-centre of the sprite bbox (the head) + clip, with LIVE-tunable settings `portrait_head_zoom_` (def 2.5, slider "Zoom tête") + `portrait_head_offy_` (def 0.10, slider "Décalage vert.") so the user dials in the face without rebuilds. Deploy-while-running trick: rename the locked ddraw.dll -> ddraw_old.dll, copy the new one (Windows allows rename/move of a loaded DLL via FILE_SHARE_DELETE, just not overwrite); loads next launch.
- Also added: `portrait_border_` (default OFF — user asked to remove the frame border; now a "Bordure" toggle) + `portrait_head_sprite_` toggle. The native equip window / cash-shop preview were the user's suggested reference for char-sprite generation (= the doll path already RE'd). Capture-hook, mirroring the RO-cursor capture in src/ragnarok/ragnarok_client.cc (Hooked_AtlasGet + g_capturing flag + InstallCursorCapture via HookManager::SetHook kJmpHook). Steps: (a) build actor obj via FUN_007ac210 with player appearance (jobid/sex/hair/colors/headgears from FUN_008cf970 reads), dir=0/action=0; (b) call FUN_007ac820(obj,1) [param_1=1 → the FUN_00a1b7c0 quad path] each frame with a capture flag set; (c) hook **FUN_00a1b7c0** `__thiscall(renderCtx, x, y, p3, sprObj, sprFrame(short*), actLayer(int*), scale(f), angle(f), color, palette(f), p11(f))` — it writes the quad into the GLOBAL scene queue (DAT_012515f8 via FUN_0053add0) and resolves the tex via SpriteAtlas_GetCachedTexture (CTexture+0x12c). In the hook, when capturing: read tex+UV (geom[3..6]) + dest + actLayer mirror([3]&1)/offset([0]/[1]) + color, store, and SKIP the original (suppress native draw); (d) ImGui-redraw the head layers in the portrait window, cropped to the head. **OPEN (validate live): the renderCtx (`*(actorObj+4)`) FUN_007ac820 needs** — vtable+0xa0 is called (color getter) BEFORE the submit, and the body/head sub-draws (FUN_007adbc0 etc.) may also deref renderCtx. Safest: live-breakpoint the EQUIP doll (FUN_007ac820 call in FUN_008b3190, equip window open) to see exactly which renderCtx fields/vtable-slots are touched, then build a minimal renderCtx (real UIWindow or a fake vtable whose +0xa0 returns white). Then write the capture.
- **Stage 3 = hide native Basic Info** (UIBasicInfoWnd id 0): wire `portrait_hide_basic_info_` to actually hide it (toggle-close via window mgr, or DrawContent no-op + clif_refresh like menu_icons' ghost fix).

Related: [[project_basic_info_menu_icons]] [[project_equip_window_re]] [[project_ro_cursor]] [[reference_imgui_game_textures]] [[project_status_tweaks_plugin]] [[reference_ui_window_manager]]
</content>
</invoke>
